
function Count(props) {    

    return (
        <span style={{ background: '#fff', color: '#000', borderRadius: '100%', padding: '20px', fontSize: '3em', textAlign: 'center' }}>
            {props.number}
        </span>
    )
}

export default Count;